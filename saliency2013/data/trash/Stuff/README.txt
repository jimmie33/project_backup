cache = temporary data, must remove after usage (keep empty)
        alternative: use /lab/ilab/1/
datasets = all dataset image and ground truth data
models = all models are set here
output = all computed saliency models here
score = all calculated score result goes here, build only a single file!
src = the code to calculate the scores here

Base Function:
calcScore = segmentation score calculation
calcScoreAUC = fixation score calculation (bad naming)